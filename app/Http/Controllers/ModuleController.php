<?php

namespace App\Http\Controllers;

class ModuleController extends Controller
{
    public function create()
    {
        // When a new module is being created, these are the things we want to take care of:
        // Every function should list its hiccups and their suggestions. A hiccup is a real-world issue that a function could face, but would not know since it is outside its module universe.
        // For example, an urgent task due today can be assigned to anybody. A hiccup arises when the person is unavailable or absent today.
    }
}
